so what do we want with this:

a lauchpad that controls the drums on a track

we want to be able to 

we want to be able to use the top half as 8 x 4 (32) max length sequencer (this is nice)
select what sound is palying with the bottom right quarter of pads (this gives us 16 different things that we can do) (maybe also with like, preview on we can play the sound by hittng the select pad, and also play in patterns with record enabled?)
and then the bottom right quarter we have 16 pads for some other stuff? - maybe it selects what pattern your editing! i like that, so the pattern of the launchpad for this track?
and the top 8 and right side 8?


